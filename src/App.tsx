import React from 'react';
import styled from 'styled-components';
const Text = styled.p`
  font-family: Arial, Helvetica, sans-serif;
`

const App = () => (
  <div>
    <Text>ToDo List</Text>
  </div>
);

export default App;
